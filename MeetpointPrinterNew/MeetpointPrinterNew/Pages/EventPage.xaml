<Window x:Name="Event" x:Class="MeetpointPrinterNew.Pages.EventPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MeetpointPrinterNew.Pages"
        xmlns:customcontrols="clr-namespace:MeetpointPrinterNew.CustomControls"
        mc:Ignorable="d"
        Title="Event" Height="900" Width="1200" ResizeMode="NoResize" 
         Style="{StaticResource HeaderFooterWindowStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="5*"></RowDefinition>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Margin="30" Background="#FFD3D4D6" CornerRadius="8" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" ></ColumnDefinition>
                    <ColumnDefinition Width="9*"></ColumnDefinition>
                 </Grid.ColumnDefinitions>
                <Image  Grid.Column="0" Margin="10" Source="/Images/cloud.png"></Image>
                <TextBlock  Grid.Column="1" FontSize="20" VerticalAlignment="Center" Margin="10" Text="Welcome Bostjan, please select the event you want to print labels for." ></TextBlock>
            </Grid>
        </Border>

        <ItemsControl x:Name="icEventItems" Grid.Row="1" Margin="30" ItemsSource="{Binding EventItems}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </Grid>
</Window>
